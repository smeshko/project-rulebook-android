package com.rulebook.core.analytics.di

import org.koin.dsl.module

val analyticsModule = module {
    // TODO: Add TelemetryDeck configuration in Story 1.6
    // single<AnalyticsService> { TelemetryDeckAnalyticsService(get()) }
}
