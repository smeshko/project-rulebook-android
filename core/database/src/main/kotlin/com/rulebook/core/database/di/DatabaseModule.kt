package com.rulebook.core.database.di

import org.koin.dsl.module

val databaseModule = module {
    // TODO: Add Room database configuration in Story 1.3
    // single { provideDatabase(get()) }
    // single { get<RulebookDatabase>().gameDao() }
}
