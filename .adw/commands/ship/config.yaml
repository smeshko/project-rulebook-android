# Phase Configuration: ship
# Generated by ADW Init Wizard
#
# Uncomment settings to customize this phase.

enabled: true

# input_files:  # Variable → file mappings for templates
#   prd: docs/prd.md
#   architecture: docs/architecture.md

llm:
  model: sonnet

# === Ship Phase Settings ===
commands:
  version_bump: ./scripts/bump-version.sh.  # Version bump command
#   publish: null  # Publish/deploy command

bypass_ci: false  # Bypass CI checks with --admin (requires admin)
wait_for_merge: true  # Wait for CI checks before merge completes
